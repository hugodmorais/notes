<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to 'Notas', root_path, class: 'navbar-brand' %>
    <div class="d-flex flex-row-reverse">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
          <% if current_user %>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#">Olá <%= current_user.email %>.</a>
            </li>
            <li class="nav-item">
              <%= link_to "Log Out", logout_path, class: 'nav-link active' %></a>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Log In", login_path, class: 'nav-link active' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>